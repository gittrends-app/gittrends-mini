{
  "name": "@gittrends/entities",
  "version": "1.0.0",
  "main": "dist",
  "types": "dist",
  "license": "MIT",
  "scripts": {
    "build": "yarn install && rm -rf dist tsconfig.tsbuildinfo && tsc && node build.mjs",
    "eslint": "eslint --color"
  },
  "dependencies": {
    "make-error-cause": "^2.3.0",
    "zod": "^3.22.4"
  }
}
