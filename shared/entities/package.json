{
  "name": "@gittrends/entities",
  "version": "1.0.0",
  "main": "dist",
  "types": "dist",
  "license": "MIT",
  "scripts": {
    "build": "yarn install && rm -rf dist tsconfig.tsbuildinfo && tsc",
    "eslint": "eslint --color"
  },
  "dependencies": {
    "joi": "^17.6.2",
    "lodash": "^4.17.21",
    "make-error-cause": "^2.3.0",
    "pluralize": "^8.0.0"
  },
  "devDependencies": {
    "@types/pluralize": "^0.0.29"
  }
}
